

// l'interface qui permet a l'utilisateur de choisir ce qu'il veut faire.
void supprimer(num , * L){


 membre* L1 = L ;
 bool trouve = false ;

 while(L1->suivant!= NULL && trouve == false)
    if (L2->num = num)
    					{
						L1->suiv=L3->suiv;
						L2->suiv=NULL;
						free(L2);
					}
         else
             if (trouve == true )
             L->suiv=L2->suiv;
             L1->suiv=NULL;
             free(L1);

				return L;
         }

